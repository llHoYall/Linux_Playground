# head

fake `head` program.

## Run

> $ mkdir build && cd build && cmake .. && make

## Usage

* `$ ./head`
* `$ ./head --help`
* `$ ./head -n 5 <files>`
* `$ ./head -n5 <files>`
* `$ ./head --lines 5 <files>`
* `$ ./head --lines=5 <files>`

